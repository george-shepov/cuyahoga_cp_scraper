# Reports samples

This directory contains very small sample report files intended for repository inclusion so CI and examples can run without large artifacts. Real reports and archives should remain local and are ignored by `.gitignore`.

Do not add real, sensitive, or large report files to this folder.
